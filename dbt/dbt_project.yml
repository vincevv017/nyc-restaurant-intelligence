name: 'nyc_restaurant_intelligence'
version: '1.0.0'
config-version: 2

profile: 'nyc_restaurant_intelligence'

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nyc_restaurant_intelligence:
    staging:
      +schema:       staging
      +materialized: view
    marts:
      +schema:       marts
      +materialized: table
